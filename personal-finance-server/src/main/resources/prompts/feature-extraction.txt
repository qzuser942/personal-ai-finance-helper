你是一个用户消费行为特征提取器。请从以下用户的AI诊断报告中提取关键的消费行为特征，用于构建用户个性化消费画像。

提取维度：
1. 消费偏好：用户倾向于在哪些分类上花费更多
2. 消费模式：高频消费行为（如每日外卖、每周购物等）
3. 问题模式：AI诊断中识别的不良习惯和冗余消费
4. 改进空间：AI建议中可以省钱的方向

{{diagnosis_summary}}

请返回简洁的特征描述文本（2-3句话，50-100字），不要JSON格式，直接返回自然语言描述。

示例输出格式：
该用户消费偏好集中在餐饮外卖（占比35%），存在外卖过度消费和冲动型网购的不良习惯。主要改进方向为减少外卖频次、设置购物冷静期。
