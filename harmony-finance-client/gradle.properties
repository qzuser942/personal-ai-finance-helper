# 限制 hvigradle JVM 堆内存，避免 Windows 页面文件不足导致 OOM
# 错误：error='页面文件太小，无法完成操作。' (errno=1455)
# 解决：减小堆 + 增加元数据空间
org.gradle.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=384m -Dfile.encoding=UTF-8
# 关闭 Gradle 守护进程，避免后台进程长期占用内存
org.gradle.daemon=false
# 并行构建关闭（单进程够用）
org.gradle.parallel=false
